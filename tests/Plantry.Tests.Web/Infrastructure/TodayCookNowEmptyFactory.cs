// File intentionally kept (referenced by legacy test infrastructure) but the factory itself
// is replaced by TodayPlannedMealsBandFactory for plantry-zp7. The old cook-now picks
// band (plantry-81g) was replaced by the planned-meals band without a layout change.
// TodayCookNowEmptyFactory is no longer consumed by fragment tests; kept as a stub
// to avoid breaking any remaining references during the transition.
